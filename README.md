# splits the gpx file into country-specific gpx files

If you have big gpx file with planty of wpt from various country you can split the file into country-specific gpx files.
This is python script that can be run with command `python geo2cntr.py <path_to_gpx_file>`

The internet connection is needed.
Lat/lon relation to country is verified online.

